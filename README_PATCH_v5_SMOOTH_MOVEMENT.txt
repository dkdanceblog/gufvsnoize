v5 smooth movement patch:
- movement is now dt-based / frame-rate independent;
- added renderX/renderY interpolation so sprites slide smoothly;
- acceleration and friction instead of instant vx snapping;
- walk cycle slowed/cleaned;
- projectile movement also uses dt;
- reduced visible walk bob jitter.
