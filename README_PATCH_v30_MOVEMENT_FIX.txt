v30 movement fix:
- Fixed a bug from v29 where updateTouchControlsVisibility() reset all movement keys every frame.
- A/D and ArrowLeft/ArrowRight work again.
- Touch controls visibility still works.
- Held controls are now released only when leaving fight/end screens, not every loop.
